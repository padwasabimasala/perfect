#!/bin/bash
# TODO start batman/spiderman ?
source $(which perfect-env)
init
addr=$(get_local_server_address)
purple Starting server on localhost:8080
cmd cd $INSTALL_DIR
cmd bash ./jboss/bin/run.sh 
